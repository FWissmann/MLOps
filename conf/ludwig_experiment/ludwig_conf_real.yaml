input_features:
    - name: ('Komponente', 'Anzahl Link/URL')
      type: number
    - name: ('Komponente', 'Anzahl Forum')
      type: number
    - name: ('Komponente', 'Anzahl System')
      type: number
    - name: ('Komponente', 'Anzahl Gefuehrte Touren')
      type: number
    - name: ('Komponente', 'Anzahl Datei')
      type: number
    - name: ('Komponente', 'Anzahl Uebersicht fuer Teilnehmer/in')
      type: number
    - name: ('Komponente', 'Anzahl Uebersicht')
      type: number
    - name: ('Herkunft', 'Anzahl ws')
      type: number
    - name: ('Herkunft', 'Anzahl web')
      type: number
    - name: ('IP-Adresse', 'nunique')
      type: number
    - name: Anzahl_log_all
      type: number
output_features:
    - name: bewertung
      type: number